# dependencies / build
node_modules
dist
frontend/node_modules
frontend/dist

# env / secrets (공개키라도 깃에는 올리지 않음 — 코드 기본값으로 동작)
.env
.env.local
frontend/.env
frontend/.env.local

# vercel / tooling
.vercel

# misc
*.log
.DS_Store
Thumbs.db
